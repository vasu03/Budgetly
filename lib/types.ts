// Create and export a new type for Transactions
export type TransactionType = "income" | "expense";

// Create and export a new type for Timeframe and Period of the Charts
export type TimeframeType = "month" | "year";
export type PeriodType = { month: number, year: number };
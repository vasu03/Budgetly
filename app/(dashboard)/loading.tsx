// Importing required modules
import React from "react";

// Creating our Loading component fo Dashboard
const loading = () => {
    
    // TSX to render the comp
    return (
       null // to be done later
    );
};

// Exporting the component
export default loading;